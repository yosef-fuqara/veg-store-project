/**
 * Storefront-only developer / studio credit (footer).
 * Set websiteUrl to a full URL to make the company name a link; leave empty for plain text.
 * logoSrc: path under public/ for a raster logo, or null to use the inline vector (project-colored).
 */
export const DEVELOPER_CREDIT = {
  name: "YoSite",
  /** Full URL (https://…) — empty string = name is not a link */
  websiteUrl: "",
  logoSrc: null,
};